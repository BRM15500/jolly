import { OnInit } from '@angular/core';
import { Router } from '@angular/router';
export declare class SubMenuContentComponent implements OnInit {
    private router;
    collection: any[];
    title: any;
    progress: any;
    nxId: string;
    private isSelected;
    constructor(router: Router);
    ngOnInit(): void;
    private toggleList(event);
}
