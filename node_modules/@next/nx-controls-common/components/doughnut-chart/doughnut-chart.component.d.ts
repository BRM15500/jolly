/// <reference types="chart.js" />
import { SimpleChange, OnChanges } from "@angular/core";
export declare class DoughnutChartComponent implements OnChanges {
    settings: {};
    nxId: string;
    chart: Chart;
    defaultSettings: object;
    constructor();
    ngOnChanges(changes: {
        [propertyName: string]: SimpleChange;
    }): void;
    setChart(settings: any): void;
    private prepareSettings();
}
