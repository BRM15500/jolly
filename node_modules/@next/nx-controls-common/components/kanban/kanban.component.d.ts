import { OnInit, EventEmitter, SimpleChanges } from '@angular/core';
import { Router } from '@angular/router';
import { Item } from '../../models/kanban-item';
import { Header } from '../../models/kanban-header';
export declare class KanbanComponent implements OnInit {
    private router;
    collection: Item[];
    columns: Header[];
    launcher_label: string;
    route: string;
    hideLauncher: boolean;
    launch: EventEmitter<any>;
    afterClick: EventEmitter<any>;
    nxId: string;
    maxHeight: any;
    data: Header[];
    itemCounter: number;
    constructor(router: Router);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    setCollection(_data: Item[]): void;
    private groupBy(_h, _d);
    private launchEvent(e);
    private cardEvent(e, i);
    private goForward(id);
    private showLauncher(index);
}
