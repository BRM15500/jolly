import { EventEmitter } from '@angular/core';
export declare class AutocompleteComponent {
    collection: Object;
    fieldsShow: string[];
    keyUp: EventEmitter<any>;
    afterClick: EventEmitter<any>;
    value: string;
    nxId: string;
    private arrowkeyLocation;
    constructor();
    keyupEvent(e: any): void;
    private ngOnChanges(changes);
    private clickEvent(e);
    private selectItem(element, index);
}
