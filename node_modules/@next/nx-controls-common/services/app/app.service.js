import { Injectable } from '@angular/core';
import { Observable } from 'rxjs/Rx';
/**
 * AppService class.
 */
var AppService = /** @class */ (function () {
    function AppService() {
        /**
         * Set up timer frequency.
         * @type {number}
         */
        this.TIMEOUT = 1000;
    }
    /**
     * Extend time value with zero if required.
     * @param value
     * @returns {string}
     */
    AppService.formatTimeNumber = function (value) {
        var stringValue = value.toString();
        return ('0' + stringValue).slice(-2);
    };
    /**
     * Get current time string.
     * @returns {string}
     */
    AppService.getNowString = function () {
        var date = new Date();
        var hours = AppService.formatTimeNumber(date.getHours());
        var minutes = AppService.formatTimeNumber(date.getMinutes());
        var seconds = AppService.formatTimeNumber(date.getSeconds());
        return hours + ":" + minutes + ":" + seconds;
    };
    /**
     * Get current time observable.
     * @returns Observable<string>
     */
    AppService.prototype.getTick = function () {
        return Observable
            .timer(0, this.TIMEOUT)
            .map(function (tick) { return AppService.getNowString(); });
    };
    AppService.decorators = [
        { type: Injectable },
    ];
    /** @nocollapse */
    AppService.ctorParameters = function () { return []; };
    return AppService;
}());
export { AppService };
//# sourceMappingURL=app.service.js.map