export declare class LogModel {
    private eventId;
    private additionalInfo;
    private applicationId;
    private userId;
    constructor(eventId: string, additionalInfo: string, applicationId: number, userId: string, created: Date);
}
