import { Observable } from 'rxjs';
import { LoginMessage } from '../models/login.model';
export declare class LoginService {
    private subject;
    private keepAfterRouteChange;
    constructor();
    gotError: boolean;
    errorMessage: string;
    errorHndlr(): Observable<LoginMessage>;
    setMessage(gotError: boolean, message?: string): void;
    message(error: boolean, message: string, keepAfterRouteChange?: boolean): void;
    clear(): void;
}
