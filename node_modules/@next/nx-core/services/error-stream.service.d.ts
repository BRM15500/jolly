import { Observable } from 'rxjs';
import { ErrorMessage } from '../models/error-message.model';
export declare class ErrorStreamService {
    private subject;
    private keepAfterRouteChange;
    constructor();
    gotError: boolean;
    errorMessage: string;
    errorHandler(): Observable<ErrorMessage>;
    setMessage(status: number, message?: string): void;
    message(status: number, message: string, keepAfterRouteChange?: boolean): void;
    clear(): void;
}
