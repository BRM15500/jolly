import { ErrorHandler, Injector } from '@angular/core';
export declare class ErrorManager implements ErrorHandler {
    private _injector;
    constructor(_injector: Injector);
    handleError(error: Error): void;
}
