import { Injectable, Injector } from '@angular/core';
import { LoggerService } from '../services/logger.service';
import { LogModel } from '../models/log.model';
import { AuthService } from '../services/auth.service';
var ErrorManager = /** @class */ (function () {
    function ErrorManager(_injector) {
        this._injector = _injector;
    }
    ErrorManager.prototype.handleError = function (error) {
        var _logger = this._injector.get(LoggerService);
        var _authservice = this._injector.get(AuthService);
        var logentry = new LogModel('error', error.toString(), 1, _authservice.getUserId(), new Date());
        _logger.Log(logentry);
        throw error;
    };
    ErrorManager.decorators = [
        { type: Injectable },
    ];
    /** @nocollapse */
    ErrorManager.ctorParameters = function () { return [
        { type: Injector, },
    ]; };
    return ErrorManager;
}());
export { ErrorManager };
//# sourceMappingURL=error-manager.js.map